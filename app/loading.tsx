// app/loading.js (or app/dashboard/loading.js)
import React from "react";

// This will be rendered automatically while the page is fetching data.
export default function Loading() {
  return <div>Loading</div>;
}
